// Create a function that takes in two arguments and returns a concatenated string in the console.

function combine(fav_food, fav_drink) {
    console.log("My favorite food is " + fav_food + " with " + fav_drink + "!");
}

combine("ice cream", "coffee")
